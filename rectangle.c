#include<stdio.h>

main()
{
	//area of rectangle
	
	int h,w,area;
    
    printf("eneter the height of the rectangle=");
    scanf("%d",&h);
    
    printf("eneter the width of the rectangle=");
    scanf("%d",&w);
    
    area= h * w;
    printf("area of the rectangle=%d\n",area);
    
}